import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container : {
        justifyContent : 'center',
        alignItems : 'center'
    },
    titleText : {
        fontSize : 40,
        textAlign : 'center'
    },
    descriptionText : {
        fontSize : 20,
        textAlign : 'center'
    }
});
